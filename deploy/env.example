# Скопируйте на сервер как .env в корне проекта (или задайте в systemd/PM2).
# Не коммитьте реальный .env.

NODE_ENV=production
PORT=3001

# Обязательно смените в проде (сессии админки)
SESSION_SECRET=случайная-длинная-строка

ADMIN_LOGIN=admin
ADMIN_PASSWORD=надёжный-пароль

# Опционально: защита POST /api/lead/webhook
# CRM_WEBHOOK_TOKEN=
